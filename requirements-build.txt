# Build-time tooling only (NOT runtime deps — keep requirements.txt clean).
# Used to produce the per-user Windows installer described in ADR-001.
#   pip install -r requirements-build.txt
#   pyinstaller build.spec --noconfirm        # -> dist\Kivun\
# Then compile installer.iss with Inno Setup (https://jrsoftware.org/isinfo.php)
# to produce WelfareSFAutomation-Setup-<version>.exe.
-r requirements.txt
pyinstaller
