# ניתוח קוד: יצירת מד התקדמות (PvProgressBar)

הקוד יוצר אובייקט של מד התקדמות (Progress Bar) ושומר אותו בתוך מילון בשם `ui`.
להלן מילון מונחים לכל פרמטר שהועבר לפונקציה:

## 1. הגדרת האובייקט והמיקום
* **`ui["Progressbar"] = ...`**:
    שמירת הרכיב החדש בתוך משתנה מילון (Dictionary) תחת המפתח "Progressbar", כדי שיהיה ניתן לגשת אליו בהמשך.
* **`pv.PvProgressBar(...)`**:
    קריאה למחלקה (Class) שיוצרת את המופע של מד ההתקדמות מתוך הספרייה `pv`.
* **`container=window`**:
    קביעת "ההורה" של הרכיב. הרכיב יוצג בתוך החלון (`window`).
* **`x=170, y=170`**:
    המיקום של הפינה השמאלית-עליונה (או המרכז, תלוי בספרייה) של הרכיב על גבי המסך (בפיקסלים).
* **`width=160, height=80`**:
    הרוחב והגובה של "הקופסה" המכילה את הרכיב כולו.

## 2. ערכים ולוגיקה (Values)
* **`min_value=0`**:
    הערך המינימלי של המד (התחלה).
* **`max_value=100`**:
    הערך המקסימלי של המד (סיום/מלא).
* **`value=0`**:
    הערך הנוכחי ההתחלתי (המד מתחיל כשהוא ריק).
* **`scale=1`**:
    קנה מידה להגדלה או הקטנה של הרכיב כולו (1 = גודל מקורי).

## 3. עיצוב ויזואלי של המד (Visuals)
* **`is_circular=True`**:
    **פרמטר קריטי:** הופך את המד מפס ישר למד עגול (Circular Progress Bar).
* **`track_color=(222, 222, 222, 1)`**:
    צבע המסלול (הרקע של המד שטרם התמלא). פורמט RGBA (אפור בהיר).
* **`track_border_color=(255, 255, 255, 1)`**:
    צבע המסגרת של המסלול (לבן).
* **`fill_color=(254, 254, 254, 1)`**:
    צבע המילוי המייצג את ההתקדמות בפועל (כמעט לבן).
* **`track_corner_radius=7`**:
    רמת העיגול של הפינות (רלוונטי יותר למד ישר, אך משפיע על קצוות המסלול).
* **`opacity=1`**:
    שקיפות הרכיב. 1 = אטום לגמרי (לא שקוף).
* **`idle_color=(255, 255, 255, 0)`**:
    צבע הרכיב במצב מנוחה/לא פעיל (כאן הוגדר כשקוף לגמרי - האלפא היא 0).
* **`track_height=12`**:
    עובי הפס של המד (כמה "שמן" יהיה העיגול או הקו).
* **`track_border_thickness=0`**:
    עובי קו המתאר (Border) של המסלול. כאן הוא 0 (ללא מסגרת).
* **`border_thickness=0`**:
    עובי המסגרת הכללית של הרכיב.

## 4. טקסט וגופנים (Text & Fonts)
* **`suffix='%'`**:
    סיומת שתופיע אחרי המספר (למשל, יוצג "50%").
* **`font='assets/fonts/OpenSans/OpenSans.ttf'`**:
    נתיב לקובץ הגופן שבו ישתמשו להצגת הטקסט.
* **`font_size=20`**:
    גודל הטקסט בפיקסלים.
* **`font_color=(255, 255, 255, 1)`**:
    צבע הטקסט (לבן).
* **`font_color_hover=None`**:
    צבע הטקסט כאשר העכבר מרחף מעליו (None אומר שלא יהיה שינוי).
* **`bold=True`**:
    האם הטקסט מודגש? (כן).
* **`italic=False`**:
    האם הטקסט נטוי? (לא).
* **`underline=False`**:
    האם יש קו תחתון? (לא).
* **`strikeout=False`**:
    האם יש קו חוצה? (לא).

## 5. מצבים ואירועים (States & Events)
* **`is_visible=False`**:
    האם הרכיב נראה על המסך בעת היצירה? (מוגדר כ-False, כלומר הוא מוסתר בהתחלה).
* **`is_disabled=False`**:
    האם הרכיב מנוטרל? (לא, הוא פעיל).
* **`on_hover=None`**:
    פונקציה שתופעל בעת ריחוף עכבר (לא הוגדרה).
* **`on_click=None`**:
    פונקציה שתופעל בעת לחיצה (לא הוגדרה).
* **`on_release=None`**:
    פונקציה שתופעל בעת שחרור הלחיצה (לא הוגדרה).
* **`tag='Progressbar'`**:
    תגית זיהוי (מחרוזת) שעוזרת לזהות את הרכיב בלוגיקה הפנימית או בלוגים.