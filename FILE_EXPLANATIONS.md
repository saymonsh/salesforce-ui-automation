# מילון מושגים: רכיב PvProgressBar

מסמך זה מהווה מקרא מלא (Legend) לכל הפרמטרים המגדירים את רכיב ה-Progress Bar (פס התקדמות) בקוד שלך. הרכיב מוגדר כטבעת טעינה מעגלית.

## 1. הגדרות בסיס ומיקום (Placement & Geometry)

| פרמטר | ערך בקוד | הסבר מפורט |
| :--- | :--- | :--- |
| **container** | `window` | **המיכל:** הרכיב חייב להיות מוצג בתוך מסגרת כלשהי. כאן הוא משויך לחלון הראשי (window). |
| **x** | `170` | **מיקום אופקי:** המרחק בפיקסלים מהצד השמאלי של החלון. |
| **y** | `170` | **מיקום אנכי:** המרחק בפיקסלים מהחלק העליון של החלון. |
| **width** | `160` | **רוחב כולל:** הרוחב של "הקופסה" הווירטואלית שמכילה את הפס. |
| **height** | `80` | **גובה כולל:** הגובה של "הקופסה" הווירטואלית. |

---

## 2. לוגיקה וערכים (Logic & Values)
אלו הפרמטרים שקובעים איך הפס מתנהג ומה הוא מודד.

* **min_value (0)**
    * נקודת ההתחלה של המד. הערך הנמוך ביותר האפשרי.
* **max_value (100)**
    * נקודת הסיום של המד (100%). כשהערך יגיע ל-100, המעגל יהיה מלא.
* **value (0)**
    * **הערך הנוכחי:** המצב ההתחלתי של הפס ברגע שהתוכנה עולה. כרגע הוא עומד על 0 (ריק).
* **suffix ('%')**
    * **סיומת טקסט:** מחרוזת שתוצג באופן אוטומטי אחרי המספר במרכז המעגל. בדוגמה זו: סימן האחוז.

---

## 3. עיצוב וצבעים (Design & Colors)
כל הצבעים מוגדרים בפורמט RGBA:
(Red, Green, Blue, Alpha-Transparency)

* **track_color `(222, 222, 222, 1)`**
    * **צבע המסלול:** הצבע של החלק הריק במעגל (הרקע שעליו "רץ" הצבע הממלא). כאן: אפור בהיר.
* **fill_color `(254, 254, 254, 1)`**
    * **צבע המילוי:** הצבע שממלא את המעגל ככל שהערך (value) עולה. כאן: לבן כמעט מלא.
* **track_border_color `(255, 255, 255, 1)`**
    * **צבע מסגרת המסלול:** צבע הגבול החיצוני של הטבעת.
* **idle_color `(255, 255, 255, 0)`**
    * **צבע במנוחה:** הצבע כאשר הרכיב לא פעיל. כאן מוגדר שקיפות 0 (בלתי נראה).
* **opacity `(1)`**
    * **אטימות כללית:** `1` אומר שהרכיב אטום לחלוטין. `0.5` היה הופך אותו לחצי שקוף.

---

## 4. סגנון צורה (Shape Style)

* **is_circular (`True`)**
    * **האם עגול?** זהו הפרמטר הקריטי ביותר.
    * `True` = יוצר מד טעינה בצורת **טבעת/מעגל**.
    * `False` = יוצר פס התקדמות ישר (ליניארי) רגיל.
* **track_height (`12`)**
    * **עובי הפס:** העובי של הקו עצמו (העובי של הטבעת).
* **track_corner_radius (`7`)**
    * **עגלגלות פינות:** רלוונטי בעיקר לפס ישר, קובע כמה הפינות מעוגלות.
* **track_border_thickness (`0`)**
    * **עובי מסגרת:** עובי הקו התוחם את המסלול. כאן הוא 0 (אין מסגרת).
* **border_thickness (`0`)**
    * **גבול חיצוני:** עובי המסגרת של הקופסה הכללית.

---

## 5. טקסט וגופנים (Typography)
הגדרות עבור המספר והאחוזים שמופיעים במרכז המעגל.

* **font**: `'assets/fonts/OpenSans/OpenSans.ttf'` (נתיב לקובץ הגופן).
* **font_size**: `20` (גודל הטקסט בנקודות).
* **font_color**: `(255, 255, 255, 1)` (צבע לבן).
* **bold**: `True` (מודגש - כן).
* **italic**: `False` (נטוי - לא).
* **underline**: `False` (קו תחתון - לא).

---

## 6. מצב מערכת (System State)
פרמטרים אלו קובעים כיצד המערכת מתייחסת לרכיב בזמן אמת.

* **is_visible (`False`)**
    * **האם נראה?** כרגע הרכיב נוצר אך הוא **מוסתר**. כדי להציג אותו למשתמש, חובה לשנות את זה ל-True בקוד בהמשך.
* **is_disabled (`False`)**
    * **האם מנוטרל?** הרכיב פעיל. אם היה `True`, הוא היה במצב "קפוא".
* **tag `'Progressbar'`**
    * **תגית זיהוי:** שם שניתן לרכיב כדי שיהיה קל למצוא אותו בין כל הרכיבים האחרים במערכת (מעין תעודת זהות).

---

## 7. אירועים (Events)
פונקציות שמופעלות על ידי פעולת משתמש. כרגע כולם מוגדרים כ-`None` (לא פעילים).

* **on_hover**: מה קורה כשעוברים עם העכבר מעל.
* **on_click**: מה קורה כשלוחצים עם העכבר.
* **on_release**: מה קורה כשעוזבים את הלחצן.