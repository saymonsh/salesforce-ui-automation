# מבנה הפרויקט והסבר קבצים

מסמך זה מתאר את תפקידו של כל קובץ בתיקיית הפרויקט `salseforce_ui`.

## קבצי ליבה והרצה (Main Application Files)

### `app.py`
זהו הקובץ הראשי שמנהל את האפליקציה.
- **תפקיד**: משמש כ"מוח" של המערכת. הוא מאתחל את ממשק המשתמש (UI), מאזין לאירועים (כמו לחיצה על כפתור "Run" או "Settings"), ומחליט איזה תהליך להריץ בהתאם להגדרות (סוג 1, 2 או 3).
- **תהליכים**:
  - אם `TYPE=1`: מריץ את התהליך מ-`login.py` (טיפול בקליטה ודיווח לפי קובץ אקסל).
  - אם `TYPE=2`: מריץ את התהליך מ-`add_candidats.py` (הוספת מועמדים לתוכנית).
  - אם `TYPE=3`: מריץ את התהליך מ-`attendance_filling.py` (מילוי נוכחות).

### `main_ui.py`
אחראי על העיצוב והנראות של החלון הראשי.
- **תפקיד**: מגדיר את הכפתורים, הטקסטים וממשק בחירת הקובץ. משתמש בספריית `pyvisual` לבניית החלון.

### `run_app.bat`
סקריפט הרצה לווינדוס.
- **תפקיד**: מאפשר להריץ את האפליקציה בקליק כפול. הוא נכנס לתיקייה, מפעיל את הסביבה הווירטואלית (venv), ומריץ את `app.py`.

## קבצי לוגיקה ואוטומציה (Logic & Automation)

### `login.py`
מכיל את הלוגיקה הראשית לטיפול בתיקים (עבור `TYPE=1`).
- **תפקיד**:
  1. מתחבר למערכת Salesforce באופן אוטומטי (כולל הזנת קוד TOTP).
  2. קורא את קובץ האקסל שהמשתמש טען.
  3. עובר על כל שורה באקסל ומבצע פעולות בהתאם לעמודה 'סוג' (1-6), כמו חיפוש ת.ז, יצירת פעילות, ויצירת דיווח שירות.
  4. משתמש בפונקציות עזר מתוך `actions.py`.

### `actions.py`
קובץ "ספרייה" המכיל פונקציות לביצוע פעולות ב-Salesforce.
- **תפקיד**: מכיל פונקציות לשימוש חוזר בדפדפן, כגון:
  - `perform_search`: ביצוע חיפוש לפי תעודת זהות.
  - `create_actions`: יצירת פעילות/תוכנית אישית.
  - `create_report`: יצירת דיווח שירות.

### `add_candidats.py`
תהליך הוספת מועמדים (עבור `TYPE=2`).
- **תפקיד**: מתחבר למערכת, מנווט לעמוד הוספת מועמדים, ועובר על קובץ האקסל כדי לסמן ולצרף מועמדים לפי תעודת זהות באופן אוטומטי.

### `attendance_filling.py`
תהליך מילוי נוכחות (עבור `TYPE=3`).
- **תפקיד**:
  1. מתחבר למערכת ומנווט לעמוד הנוכחות.
  2. מחכה לאישור ידני מהמשתמש (כפתור "המשך" שמוזרק לדף).
  3. עובר על טבלת המשתתפים ומסמן "נוכח" עבור כולם באופן אוטומטי.

## קבצי הגדרות (Configuration & Settings)

### `parameters.py`
קובץ המכיל את הגדרות המערכת.
- **תפקיד**: שומר משתנים כמו שם משתמש, סיסמה, מפתח סודי (ל-TOTP), כתובות URL, וסוג הפעולה (`TYPE`). הקובץ מתעדכן כאשר שומרים שינויים דרך מסך ההגדרות.

### `settings_ui.py`
אחראי על העיצוב של חלון ההגדרות.
- **תפקיד**: בונה את הטופס שבו המשתמש יכול לערוך את הסיסמאות וההגדרות שנשמרות ב-`parameters.py`.

## קבצים נוספים

- **`__init__.py`**: קובץ ריק שמסמן את התיקייה כחבילת Python (Package).
- **`selenium_session.txt`**: קובץ טקסט לשמירת נתוני סשן (יתכן שאינו בשימוש פעיל בקוד הנוכחי או משמש לגיבוי).
